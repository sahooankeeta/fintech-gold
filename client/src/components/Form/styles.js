import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
  paper: {
    display: "flex",
    flexDirection: "column",
    padding: "10px",
    alignItems: "center",
  },

  avatar: {},
  form: {
    width: "100%",
    padding: "15px",
  },

  input: { marginBottom: "10px", width: "90%" },
}));
